#include <bits/stdc++.h>

// (user id, process id, node id, device id, function id, arguments dimensions,

struct Server {
  int userId;
  int processId;
  int nodeId;
  int deviceId;
  int functionId;
  int argDim; // probably want this as a list of some kind
};
